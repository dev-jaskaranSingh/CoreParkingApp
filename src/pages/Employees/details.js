import React from 'react';
import { StyleSheet, Text } from 'react-native';
import Layout from '../Layout';
const Details = () => {
    return (
        <Layout>
            <Text>Details Employee</Text>
        </Layout>
    );
}

const styles = StyleSheet.create({})

export default Details;
